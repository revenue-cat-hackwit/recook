import { View, Text } from 'react-native';

export default function Generate() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-lg">AI Generate Resep dari Video Screen</Text>
    </View>
  );
}
